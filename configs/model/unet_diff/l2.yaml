defaults:
  - base
  - _self_

in_channels: 1
out_channels: 1
image_size: 128
attention_resolutions: [4]  # String -> list of int
channel_mult: [1, 1, 2, 3, 4]
model_channels: 32
pretrained: ${paths.weights_dir}/ddpm_unet_diff_ct_l2/best_val.pth
