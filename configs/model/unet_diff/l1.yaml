defaults:
  - base
  - _self_

in_channels: 1
out_channels: 1
image_size: 256
attention_resolutions: [8]  # String -> list of int
channel_mult: [1, 1, 2, 2, 4, 4]
pretrained: ${paths.weights_dir}/ddpm_unet_diff_ct_l1/best_val.pth
